// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyBuUjQaqRdQF91aOdwRV_bJO8MB09XLFRc',
    authDomain: 'angulardebug.firebaseapp.com',
    databaseURL: 'https://angulardebug.firebaseio.com',
    projectId: 'angulardebug',
    storageBucket: '',
    messagingSenderId: '339279377213',
    appId: '1:339279377213:web:1a1ee47733d1152a'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
